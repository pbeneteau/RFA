# Project status and handoff

Last updated 2026-08-30. **Read this first when resuming work.** Live counts come only from running the gates (`npm test` prints its count, `npm run e2e` writes `reports/latest.md`, `rfa evals run|parity` print theirs); no document states one.

- **State**: protocol 0.1.9 (draft; wire Appendix F in `spec/RFA-0.1.md` is the single per-requirement status table); specs RFA-0.4 through RFA-0.9 in force (RFA-0.9, egress and the declared surface, accepted 2026-08-29); the owner's instance is the hub directory `~/rfa/acme` (the Mac Studio test project: `~/Dev/rfa-test`). Rung status lives in THIS file's three rung lists below, never in a spec. The merged v0.5/v0.6 ladder (content in spec/RFA-0.5-platform.md sect. 22): every locally buildable rung SHIPPED, the admission half parked per the decisions below; its only open remainders are already in the open-items list.
- **Decisions in force**: the audience is any organization self-hosting, never a personal tool (2026-08-17). The repository stays private and CI stays declined, do not re-offer (2026-08-19). Direction review: read the gitignored `reports/direction-review-2026-08-21.md` before any strategy work (2026-08-21). The sixty-day remote decision was re-ruled: a counterparty exists, remote MECHANICS un-parked, admission HARDENING parked until the first uncontrolled peer or a release (2026-08-21). The run, never the turn, is the unit of parallelism, and the turn lock (`src/turnlock.ts`) is permanent architecture (2026-08-25).
- **Open items**: the labelling sitting (Paul's: the dashboard's Evals tab, or `rfa evals label --prepare` then `--apply`); the handbook clone, one `rfa knowledge add` command awaiting Paul's GitLab credential (context in docs/HISTORY.md); the watchdog is DONE and closed (both halves of 20.6, 2026-08-30; see "the watchdog" below); `redact` plus retention/export; the mixed local+remote test week; the live stack runs the RFA-0.9 build (restarted 2026-08-30, `rfa doctor` clean, real product questions answered with file citations at ordinary latency and cost, and `linear-scribe`'s Linear server now starting inside its own OS sandbox) and `pm-agent` STANDS at `concurrency: 2`, which `pm-live-05`'s overlap assertion depends on, so reverting it means removing that line; parity is 4/4 twice clean plus once under genuine parallel load, and the reliability gate is 6/6 clean at pass^4 band 0.15 with a measured flake rate of 0.0% over 36 trials (2026-08-30), so the turn lock and the fence are trusted; and the day-one-offline question is ANSWERED (2026-08-27): every scaffolded kind's own eval case now ships inert, so a fresh instance's first `rfa evals run` needs no credential.
- **Wire conformance**: the amended `core` profile of 0.1.8 is MET as of 2026-08-30, ahead of the 0.7.0 deadline 16.1 sets (this is 0.6.4). It took three passes and the last one caught a regression in the second: the `16.1` row blamed a blocker 16.1 says cannot be one, a code check named three, and attacking the claim that those three were fixed found an in-stream compaction marker had broken chain verification and that sect. 15's argument validation had been a fourth blocker all along. The items were 9.3's `mentions` filter, 14 item 11's application (three peer-text paths into a model prompt), 14 item 12 (both halves), 9.1's compaction summary - delivered BESIDE the events on the owner's ruling, because an in-stream marker breaks a verifier - and sect. 15's `bad_request` on argument validation, which the MCP SDK used to answer with prose. All in [docs/LEDGER.md](docs/LEDGER.md). Half B stays gated on a named counterparty, which 16.1 says a guestless hub must not be judged on. **One KNOWN MISS is open and is an owner's call**: 9.3's "reply to a message the caller sent" clause is bounded by a 500-entry `sentIds` cap, so an UNADDRESSED reply to an old message of yours stops matching past 500 sends (no conforming client meets it - the reference client and the SDK both set `to` on a reply, which matches first). Unfixed because the fix is a tradeoff: unbounded memory on a weeks-long resident, a higher cap that only moves the cliff, or a time window that changes which case fails. `test/hub.test.ts` pins the behaviour so a change has to be deliberate.
- **Moved content**: the findings ledger is `docs/LEDGER.md` (new findings go there directly); completed-work narrative is `docs/HISTORY.md`.

## The watchdog (RFA-0.5 sect. 20.6, both halves, 2026-08-30)

- The invariants live in `src/watchdog.ts` and NOWHERE else. `scripts/watchdog-replay.ts` (the validation gate) and `src/supervisor.ts` (the runtime) both import them; a test fails if a second definition reappears in the harness. Validated as one copy and shipped as another validates nothing.
- **Shipped: `engine-run-stuck-running` only.** A run in `running` for more than 2h - four times the longest legitimate wait, the 30-minute ask deadline. Replay over 14.1 days: 54 firing buckets, 54 inside a recorded incident, 0 outside, which is exactly 20.6's standard. The naive observer-lease form FALSE-FIRES (20.6 forbids it) and its corrected form is SILENT (never shown to fire on anything, so never validated); both stay defined, unshipped, with their measured verdict, so shipping one later is a re-replay rather than a rewrite.
- **The verdicts are re-measured, not trusted.** `verdictDrift()` compares the corpus against what the module claims and the harness exits 1 on any disagreement, an unreplayed invariant included. Re-run it as the corpus grows: `npx tsx scripts/watchdog-replay.ts --dir ~/rfa/acme`.
- **It alerts, it never sweeps.** `reconcileDead` refuses to infer death from age; the watchdog applies that test to what the sweeps leave behind and hands the decision to a human. Two readers: the supervisor's ops timer into `#ops` (5-minute pass, 30-minute per-invariant cooldown) and `rfa doctor`, which reads the same rows over a channel that cannot 401.
- **No shipped invariant is blind.** The supervisor passes `rosters: null` because the hub owns the room store exclusively; `watchdogFailures()` names any shipped invariant needing what a caller cannot read, and the supervisor logs it at start. Empty today, checked so it cannot become non-empty quietly.

## v0.8: concurrency (accepted 2026-08-25; rung status lives HERE, never in the spec)

The spec is [spec/RFA-0.8-concurrency.md](spec/RFA-0.8-concurrency.md), drafted from research wave 05 ([research/05-concurrency/REPORT.md](research/05-concurrency/REPORT.md)); its wire half is transplanted into protocol 0.1.9 (draft). Build order per spec sect. 15: rungs 1 and T together, then 2, then 3, then 4, then 5, then 7, with 6a and 6b trigger-gated outside that sequence and blocking nothing; diffs touching the named concurrency surfaces run the second parity pass under parallel load (sect. 14).

- Rung 1, the correctness floor: DONE 2026-08-26 (FactStore transaction + unique live-hash index with collapse-by-expiry; consolidation single-flight + CAS watermark; per-turn lease set with keepalive-renews-all; `reconcileDead` on the supervisor's timer; create-exclusive with conflict files and `expected_hash` preconditions; gate skips recorded). Findings in [docs/LEDGER.md](docs/LEDGER.md).
- Rung T, the testing rung, parallel to rungs 1-3: DONE 2026-08-26 (four seams in `test/interleaving.test.ts` with the claim seam in `src/store.ts`; the e2e canary "two asks against one pack"; the parity-under-load rule is in CLAUDE.md, not a new gate). Its first run found a LIVE regression unrelated to v0.8: knowledge paths were rendered against a different base than the model resolves them against, which had made out-of-pack knowledge unreachable since 2026-08-23 ([ledger](docs/LEDGER.md)).
- Rung 2, runtime hygiene: DONE 2026-08-26 (the account slot is LENT across both blocked waits, nested ask and approval card, with presence and the heartbeat untouched; chain ids, the depth cap and the `would_deadlock` refusal from inside the ask-wait loop; the hub-defaulted cross-home `reply_by` at 600 s; approval-card consumption as a canonical action identity plus a uniqueness-constraint claim in `runs.db` with effect classes and an idempotency key; the effective cap default standardized at 2). Wire Appendix F carries the two per-requirement rows; findings in [docs/LEDGER.md](docs/LEDGER.md).
- Rung 3, read-only parallelism: DONE 2026-08-26 (the dispatcher in `src/dispatch.ts` with per-conversation FIFO, one running job per conversation key, bounded queues refusing the arrival, and deadline admission checked at submit AND at dequeue; the turn lock narrowed to one turn per session id; reservation-then-settle budget admission inside `acquire()` with `agent_spend`, `lease_settlements` and cost on every error path; `concurrency: N` behind its three gates plus the fence check at resident startup; sect. 4 item 2's memory-verb ownership split, without which gate 2 was undecidable; the knowledge-sync drain barrier and the HEAD taint stamp; the e2e canary flipped to overlap-allowed). The design is [docs/design/rung3-dispatcher.md](docs/design/rung3-dispatcher.md), written before any edit as sect. 6.2 requires; findings in [docs/LEDGER.md](docs/LEDGER.md). No wire Appendix F row changed: every one still PENDING belongs to rung 7.
- Rung 4, candidate parallelism: DONE 2026-08-26 (the fan-out in `src/candidates.ts` with the ledger asked its width before any candidate starts; one candidate set id on every run so cost per TASK is a query; per-candidate scratch under the pack, the losers' deleted; the two selection variants, human first and early-stop cancelling by `query.interrupt()` so a killed candidate still settles its real cost; `candidates: N` in the pack schema behind concurrency's gates; `rfa task create --candidates`, `rfa task candidates`, `rfa task select` and the dashboard's `c` picker; only the winner speaks in the room, and only a selected winner reaches memory). The fan-out is LOCAL, not wire-visible, and spec sect. 11 carries the clarifying amendment that decision owes; no wire Appendix F row changed. The design is [docs/design/rung4-candidates.md](docs/design/rung4-candidates.md); findings in [docs/LEDGER.md](docs/LEDGER.md).
- Rung 5, writing packs on scratch dirs: DONE 2026-08-26 (the two-door fence in `src/writefence.ts` and `src/fenceprobe.ts`: guarded built-ins out of `allowedTools` for every pack, the SDK's shadowing warning read and fatal on the guarded set only, a live per-built-in deny probe at every boot with `bypassed` and `inconclusive` both fatal, door one's per-run path guard on realpath'd paths plus the task claim-fence check, door two as the SDK's per-`query()` `sandbox` option with the run's `scratch/<runId>` as its cwd and allow root, and a writing pack that cannot establish both doors refusing to boot). Both preconditions discharged by measurement: Edit's interception (probes C-E) and per-run door two (probes F-J, which also found `dangerouslyDisableSandbox` open by default). Spec sect. 9 carries the amendment those probes owed, labelled A1 onward so that its items 1-3 stay the three doors every existing citation means; no wire Appendix F row changed. Design note [docs/design/rung5-writefence.md](docs/design/rung5-writefence.md), live proof `npm run fence-proof` (8/8), findings in [docs/LEDGER.md](docs/LEDGER.md).
- Rung 6 SPLIT on 2026-08-26 after both its preconditions were checked and failed (detail in [docs/LEDGER.md](docs/LEDGER.md)). **Rung 6a, the per-run clone workspace: NOT STARTED, unblocked** (a CoW clone needs no repository), trigger is a pack wanting per-run isolation without merge-back. **Rung 6b, the publish lifecycle: NOT STARTED, trigger-gated**, and it does not block rung 7. Of its two original blockers only the DESIGN one is resolved, and only on paper: the repository-ownership fork is DECIDED in spec sect. 8.2 (RFA owns one git dir per pack under `.rfa/packs/<pack>/git/`, work tree the pack directory, no `.git` in the pack tree, ignore rules in that repository's own info/exclude, created lazily, never a `git init` or an adoption in the operator's visible tree, the reconcile loop may never discard a change it did not author, and its dirty-tree refusal owes an operator-owned `rfa agent publish --adopt` exit that 6b must ship with it); `sandbox.isolation` no longer lies either (it was inert, accepting values nothing read; it now refuses each by name, and spec sect. 8.1 amends v0.4 sect. 3.2's vocabulary). The other original blocker, the TRIGGER, is still open: no pack on this instance writes to its own tree, since `pm-agent` is read-only and `linear-scribe` writes to Linear, so rung 5's scratch surfaces serve every writing pack that exists. Before building 6b, classify the trigger first: if the pack's work is a repository the OPERATOR owns rather than its own tree, that is the parked external-workspace shape in spec Appendix A, not this rung.
- Rung 7, the fleet: **HALF ONE DONE 2026-08-26, and the hub advertises `spec_version: 0.1.9` because of it.** The board's resource claims are live: `resources[]` on `room_task claim` with the authority-segment grammar, NFC and the 256-byte/16-key bounds as `bad_request`, prefix-or-equal intersection ON SEGMENTS refused with `task_conflict` and never queued, widening as a claim by the current owner that never damages the grant it holds, the creator-approved reservation after three refused widenings, grants persisted on the task and dropped at every release trigger and terminal state, the `hmac-sha256:` digest for a non-local claimant blocked by a `local/…` key, `lease_expired` thrown with its data, and the fence threaded to rung 5's door one so a write whose grant is gone is refused at the tool. **Half two is PARTIAL**: `max_claims_per_member`, `task_actions_per_min` (keyed per RFA-0.6 sect. 5.6), the greedy-peer watch (3 flaps / 600 s, auto-hold off, surfaced as the new `greedy_release_watch` system event) and the cross-org memory quarantine all ship. Every member on this instance is `home: "local"`, so those branches were unit-tested and unexercised until 2026-08-27, when seven scenarios in `scripts/e2e.ts` (5c to 5i) began driving them against a real hub PROCESS over the wire, with the home forced into a disposable hub's stopped store as a labelled test-only stand-in for an admission record. That found three things and proves nothing about interop with a real counterparty, which stays parked; the first of them is CLOSED on 2026-08-28 by the owner's ruling that grant keys are redacted per reader (wire 10.3 item 7 amended, `taskForReader` in `src/store.ts`, asserted in scenario 5e), so the board no longer publishes the layout item 8 digests; the entry in [docs/LEDGER.md](docs/LEDGER.md) has both halves. Wire Appendix F flipped five rows and shrank a sixth; protocol Appendix B gained one system event name, recorded in RFA-0.8's changelog. Design note [docs/design/rung7-fleet.md](docs/design/rung7-fleet.md); findings in [docs/LEDGER.md](docs/LEDGER.md).

## v0.9: egress (accepted 2026-08-29; rung status lives HERE, never in the spec)

The spec is [spec/RFA-0.9-egress.md](spec/RFA-0.9-egress.md), drafted from [research/06-agent-fabric/REPORT.md](research/06-agent-fabric/REPORT.md) (a landscape comparison, load-bearing for nothing) and from twelve live probes. Build order per sect. 11: 1, 2, 3, 4, then 5/6/7 in any order, then 8. A diff touching door two's policy construction, the coverage predicate, door one's branches or any `query()` call site runs `npm run fence-proof` AND `npm run egress-proof` (sect. 12 item 1); neither belongs in `npm test`.

- Rung 1, the inert fields stop lying: DONE 2026-08-29. `sandbox.network` and `sandbox.allowed_domains` had zero readers while every scaffolded pack carried `network: none`. The scaffold stopped writing it, `rfa agent edit <name> --drop-network` strips it from packs already on disk through the same validated `editPack` write, and `rfa doctor` names the pack and that fix. Discharged 4.6, and 4.2's `open` refusal-by-name.
- Rung 2, the platform's own lanes declare their surface: DONE 2026-08-29. `llmOnce` (consolidation and reflection, on untrusted room episodes) now passes `tools: []` and `settings: { disableClaudeAiConnectors: true }` and takes NO cwd at all - it mints an empty temp directory per call, so no caller can point it at the hub root again (E9 read a file there; E12 measured the read executing under the production `maxTurns: 1`). `src/querysites.ts` is the enforced inventory of every `query()` call site, anchored on the IMPORT; a module that reaches the SDK's `query` and is not listed fails `npm test`. Discharged 6.1, 6.2, 6.3.
- Rung 3, the class table and the coverage predicate: DONE 2026-08-29. `src/toolclass.ts` classifies every pre-approvable built-in (`guarded`/`command`/`reach`/`read`/`subagent`, matched on the tool HEAD); an unclassified `tools.allow` entry, an empty one, a duplicate, and an `mcp__` name for an undeclared server are all refused at definition load. The fence's coverage predicate is `fenceApplies` (guarded OR command), so a pack declaring `Bash` and no guarded built-in - which used to get NO door at all - is fenced. `sandbox.cwd` must resolve inside the pack directory and is refused outright on a fenced pack. RFA-0.8 sect. 9 item 2 and CLAUDE.md carry the amendment 3.6 owes. Discharged 3.1-3.4b and 3.6.
- Rung 4, the feature: DONE 2026-08-29. `src/egress.ts` reads the posture: `none` is an EMPTY allowlist with `strictAllowlist: true` (never an omitted key, which is the ask path E10a measured returning HTTP:200), `allowlist` needs non-empty domains and a command surface, `open` is refused by name permanently, an `interrupt_on` rule matching `SandboxNetworkAccess` is refused, and door one's branch for that name is a fail-closed BACKSTOP whose arrival fails the run. The boot establishment asks srt for the PACK's policy (it asked for `network: {}` before, proving nothing) and checks the refusal carries the allow-list reason and not `user denied`. `npm run egress-proof` is the live half. Discharged 4.1-4.5, 4.7 and 10.2.
- Rung 5, the declared surface: DONE 2026-08-29. `reachableToolSurface` is the one function for a pack's TOTAL reachable tools, platform-injected servers included; above 25 it warns at definition load and in `rfa doctor`, naming the count and whose number it is; an unknown key in an `offers` entry is refused instead of silently discarded. Discharged 7.1-7.3.
- Rung 6, capability lifecycle and artifact drift: DONE 2026-08-29. `deprecated` and `superseded_by` on an offer, with `selectOffers` as the one place the rule lives; `rfa connect --skill` records the absolute destinations it wrote into another repository, the (pack, definition hash) pairs the hint came from, and any pack the tolerant scan skipped as broken, and `rfa doctor` reports drift on every recorded destination still on disk. Both carry the locality caveat 8.1 demands: a roster selector cannot see the flag. Discharged 8.1 and 8.2; 8.3 stays a deliberate no-wire-delta.
- Rung 7, the renderings: DONE 2026-08-29. `src/surface.ts` names every unconfined surface a pack holds (`read` built-ins, `reach` built-ins, each MCP server by form, the platform's own injected tools, an unproven `allow_subagents`), with the mechanism for each; `rfa agent show`, `rfa doctor` and `rfa status` render the posture only ever WITH its scope sentence, and each says the value is from disk because a resident records none of it. A pack declaring `WebFetch`/`WebSearch` must carry `unconfined_reach_acknowledged: true` or the definition is refused. Discharged 3.5, 5.2, 5.3 and 10.1.
- Rung 8, MCP-server confinement at the `mcpServers` spawn seam: DONE 2026-08-30, after the owner's decision that the policy is **declared per server, never derived** (sect. 4.1 says the pack posture does not govern MCP servers, so reusing it would make that scope sentence false everywhere it is rendered). `mcp_servers.<name>.sandbox` carries `network`, `allowed_domains` and `allow_write`; a `command` or `builtin` server that declares none is REFUSED at definition load, and a `url` server that declares one is refused by name. Each server is spawned through `src/mcplaunch.ts`, which establishes ITS OWN srt instance and execs the real server inside it with stdio inherited - one instance per server, because `SandboxManager` is a process-global singleton with one egress proxy and one allowlist. The launcher refuses to run rather than running a server unconfined. `npm run egress-proof` measures the pair: the same stdio child escapes unwrapped (reaches a host `Bash` could not, writes outside the allow root) and is confined when launched. The residual surface rung 8 could not cover is the `url` form, named as such by `rfa doctor` and `rfa agent show`, the two renderings sect. 5.2 binds, along with what stays unconfined even for a wrapped server: the ARGUMENTS of the tools the pack declared. The live `linear-scribe` pack was updated and re-verified: it boots, the real builtin server completes its MCP handshake inside the sandbox, and the pack serves. Discharged 5.4.

## 2026-08-27: the instruments and the CLI caught up with the ladder

- **The reliability gate tests concurrency now**, rather than only proving concurrency did not break serial answers: a `live-concurrent` case kind where one trial is a simultaneous PAIR of asks from two probes, each answer checked against a marker the SIBLING's correct answer is certain to carry, with distinct run ids asserted. Its first overlap metric COULD NOT FAIL (it intersected the client's send-to-reply windows, which all begin in one tick) and was reported as proof anyway; it is rebuilt on the subject's own run rows from `runs.db`, fails closed when unmeasurable, and was then disproved by a live A/B on the pack. The seeded tenant-neutral pair ships INERT as `case.yaml.example`, so a fresh hub's first `rfa evals run` still scores offline. Both stories in [docs/LEDGER.md](docs/LEDGER.md).
- **The CLI can see what the ladder built.** `rfa doctor` gains the write-fence declaration check, door two ESTABLISHED by running a sandboxed write rather than asked about (door one stays UNVERIFIED by design and can never print ok), the concurrency gates, definition drift, and what the RUNNING supervisor could not load; `rfa task show` renders resource grants with an opaque digest labelled as one and an expired lease saying so; `rfa agent show` and `rfa status` show concurrency; aliased rooms sort ahead of the historical ones.
- **One bad `agent.md` no longer unsupervises the fleet.** The supervisor reconciles per pack, naming the broken one and leaving its resident exactly as it is, and `rfa up` and `rfa down` stopped dying on it. Note the trap that makes the naive fix worse than the bug: a broken pack dropped from the declared set reads as RETIRED, so its healthy resident gets drained. One tolerant scan (`scanPacks`, `declaredPackNames` in `src/agentdef.ts`) serves the supervisor and the CLI; `listPacks` still throws for the dozen callers that want it loud.
- **A working rule was added to CLAUDE.md**, earned three times in two days: a check comparing CONFIGURED against HAPPENING must read the running system's own record, never the file the operator edited. All three of those defects made their instrument MORE reassuring, so no passing test could find them.

## 2026-08-28: the guest half runs, and two disclosure defects it found

- **Rung 7's guest-facing branches execute against a real hub for the first time** (seven scenarios in `scripts/e2e.ts`, run under `npm run e2e:full`). They reach it through `forceHomesInStoppedStore`, a labelled test-only stand-in that writes `home` into a STOPPED store so the hub serves the membership back as a guest on restart: it admits nobody, checks no credential and binds no transport principal, and the stable peer identity the fairness scenarios need is not forged at all, coming from `--human-key` plus `human_key` at join. **It proves the code paths behave, NOT that a counterparty's framework interoperates**; the cold-start stranger test remains the only thing that proves interop and remains parked with admission. Its structural blind spot is stated in the scenario and the ledger: anything decided AT JOIN TIME is only ever exercised with `home: "local"`, because the home is forced after the join.
- **Two disclosure defects, both reachable only with a guest present, which is why neither had been seen.** `seesBack` had no home condition, so a guest joining a `history_visibility: "member"` room and asking for the permitted `history_limit: 500` would have been handed up to 500 pre-join events, against wire spec line 194; hardened. And the board handed a guest every live grant's RAW resource keys while the refusal path carefully digested the same layout, which made wire 10.3 item 8's digest protect nothing while looking like a protection: grants are now redacted per reader by authority segment, `room/<handle>/...` and the guest's own home verbatim so back-off still works, everything else through item 8's own digest helper, local readers untouched.
- **The redaction broke the hash chain for a guest, and that was found by reading the spec rather than by a failing test.** Section 13 says only appended fields participate and "beyond that a verifier needs NO field surgery"; the redaction mutates an appended field, and `wait_for: "all"` gives a guest the contiguous stream verification needs. A changed task event now carries `content_hash`, the hash of its appended form, reusing 12.1's field deliberately because on the one event where both producers meet they want the same value. `linkOf` in `src/chain.ts` had to test the FIELD rather than `redacted: true`, or it would have rejected the stamp in front of it.
- **Wire Appendix F's 5.1 row was overclaiming** and now says which half holds: leave-and-rejoin DOES reset a guest's budget today, because the key falls back through `peer_id ?? principal ?? member.id` and `peer_id` belongs to the parked admission record.
- Owner-facing leftovers from this work, all recorded in [docs/LEDGER.md](docs/LEDGER.md) rather than silently carried: `redact` (12.1) still has no producer, so the overlap with the new stamp is unit-tested only; `room_watch` push delivery is stamped but proven end to end only over `room_listen`; and `INTEROP.md` changed, which is the counterparty-facing artifact and the one edit worth an eyeball.

## v0.7: the operator CLI and the hub directory (accepted 2026-08-22; rung status lives HERE, never in the spec)

The plan is [spec/RFA-0.7-cli.md](spec/RFA-0.7-cli.md), accepted 2026-08-22 with its section-10 recommendations as written. One line per rung; every DONE rung's full story is in [docs/HISTORY.md](docs/HISTORY.md).

- Rung 0, the hub directory: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-0-the-hub-directory-done-2026-08-22)).
- Rung 1, `rfa` and the lifecycle: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-1-rfa-and-the-lifecycle-done-2026-08-22)).
- Rung 2, agents and rooms: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-2-agents-and-rooms-done-2026-08-22)).
- Rung 3, reach: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-3-reach-done-2026-08-22)).
- Rung 4, instruments and duties: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-4-instruments-and-duties-done-2026-08-22)).
- The owner's instance migrated to `~/rfa/acme`: DONE 2026-08-22 ([story](docs/HISTORY.md#the-owners-instance-migrated-done-2026-08-22)).
- Rung 7, the front door: DONE 2026-08-22 ([story](docs/HISTORY.md#rung-7-the-front-door-plan-sect-13-amendment-of-2026-08-22-done-2026-08-22); [second pass](docs/HISTORY.md#the-front-door-second-pass-2026-08-22-from-the-owners-first-hour-with-it-done)).
- Agent modes, RFA-0.4 sect. 3.12: DONE 2026-08-22, `auto` withdrawn 2026-08-23 ([story](docs/HISTORY.md#agent-modes-rfa-04-sect-312-amendment-of-2026-08-22-done-2026-08-22)).
- The gate run for real on the Mac Studio test project: DONE 2026-08-23 ([story](docs/HISTORY.md#the-gate-run-for-real-on-the-mac-studios-test-project-2026-08-23)).
- The Evals tab, the labelling sitting in place: DONE 2026-08-23 ([story](docs/HISTORY.md#the-evals-tab-the-labelling-sitting-in-place-2026-08-23-done)).
- `rfa agent edit` as a walkthrough: DONE 2026-08-23 ([story](docs/HISTORY.md#rfa-agent-edit-as-a-walkthrough-2026-08-23-done)).
- The Tasks tab: DONE 2026-08-23 ([story](docs/HISTORY.md#the-tasks-tab-2026-08-23-done)).
- A whole-CLI review, every finding fixed: DONE 2026-08-24 ([story](docs/HISTORY.md#a-whole-cli-review-and-every-finding-fixed-2026-08-24-done)).
- First-use follow-ons (task-by-capability, `rfa ask --reply`): DONE 2026-08-24 ([story](docs/HISTORY.md#first-use-follow-ons-2026-08-24-done)).
- Reflection, rung A (RFA-0.4 sect. 5.4): DONE 2026-08-24 ([story](docs/HISTORY.md#reflection-rung-a-2026-08-24-done)).
- The three-layer tool fence (bypass mapping, harness-internal tools, account connectors): FOUND LIVE AND FIXED 2026-08-24 and 2026-08-25 ([story](docs/HISTORY.md#bypass-was-wider-than-the-pack-2026-08-24-found-live-fixed)).
- Rung 5, the guest path: NOT STARTED, gated exactly as v0.5 sect. 22 rung 11 gates it.
- Rung 6: NOT STARTED, needs the publishing decision (plan sect. 10, owner) and a week of use.

## Runbook (after a reboot or to resume)

The instance is a HUB DIRECTORY, never this checkout (RFA-0.7): the owner's is `~/rfa/acme`, the Mac Studio's test project is `~/Dev/rfa-test`. Every command below runs from inside one, or is told with `--dir` / `RFA_DIR`. This repository carries no `agents/`, no `data/` and no instance of any kind; `templates/evals/` is what `rfa init` seeds a new one from.

```bash
rfa                      # the front door: the dashboard here, the onboarding in a folder that is not one yet
rfa up                   # hub + supervisor as daemons; rfa status names them, rfa down stops them
rfa doctor               # every check the findings ledger paid for, each naming its scar
```

Health check: `curl -s http://127.0.0.1:<port>/healthz` answers `{"ok":true}` and needs no credential (RFA-0.6 sect. 8.7). It returns 503 with `Retry-After` while the hub is draining or if another hub has taken its store lock, so it is the right thing for a monitor or a container healthcheck to poll. Do NOT read a 200 from `/console` as health, which is what this runbook used to do: that only proves a file could be read.

Reaching it from a phone or another machine: `rfa hub expose --tailscale` fronts the loopback listener with the tailnet name and records it as `hub.public_url` (then `rfa restart`, which the hub reads at start). The listener stays on loopback on purpose: a proxy terminates identity, a wider bind does not. The public name must be an allowed origin or the browser's `Origin` is refused with 403 by the DNS-rebinding allowlist, which looks exactly like a wrong credential (see the findings ledger); `hub.public_url` is what allows it.

No credential is ever on argv. `.rfa/secrets.json` (0600) holds the operator's own copies and the CLI reads them per call; `rfa secrets set <NAME>` prompts with echo off, or takes `--stdin` / `--from-env`. A `--human-key` on a command line was found sitting in `ps aux` in full on 2026-08-17.

**Backups and restore**: `rfa backup now` writes the dated backup the supervisor writes nightly (SQLite `.backup` copies plus one archive of the room logs, supervisor state, the four runtime files and every pack's memory) into `retention.backup_dir`, outside the hub directory on purpose. `rfa backup ls` lists them; `rfa backup restore <day>` refuses while the hub or the supervisor is running (a database replaced under a process that holds it open is corruption, not a restore), writes a `pre-restore-<instant>` safety backup first, and drops stale `-wal`/`-shm` beside a restored file. The procedure was exercised 2026-08-17 in its pre-0.7 form.

**Agent lifecycle is a CLI, never a copy-paste** (2026-08-19, now `rfa`):

```bash
rfa agent new                       # alone on a terminal: the walkthrough, every setting with its reason
rfa agent new my-agent --kind tool --builtin linear   # or headless, every answer a flag
rfa agent edit my-agent             # the same walkthrough over an existing pack; --editor opens agent.md
rfa agent mode my-agent ask|plan|bypass
rfa agent retire my-agent           # stop, release leases, leave, evict, archive, deregister: eight re-runnable steps
```

The scaffold writes what hand-written packs got wrong here: `RFA_TOKEN` in `secrets` (without it a resident fails with an opaque `unauthorized`), a skill on the card (a participant join is refused without one), budgets (a pack with neither ceiling spends unbounded and warns once), `allow_subagents: false`, and a room binding. It refuses a reserved first token (human, console, system, hub, rfa) with the reason and a suggestion rather than letting the hub refuse it at join, and validates through the same zod schema the supervisor uses, so a generated pack cannot be one the platform then rejects. A resident's tool surface is its declaration and three mechanisms enforce that (RFA-0.4 sect. 3.12): read them before widening one.

Residents live in `<hub directory>/agents/<name>/` (agent.md + knowledge + state); the supervisor watches definitions and does a versioned drain on edit, so the room sees the digest rotate. Logs: `rfa logs hub|supervisor|<agent> -f`. Boot persistence: `rfa service install` (launchd or systemd units running `rfa hub run` and `rfa supervisor run`; refused while `rfa up` daemons are alive). Brain or knowledge changes must pass the parity gate: `rfa evals parity`, twice.

Human principals (0.5.0): joining with a human key grants `origin: human`, required for `role: supervisor`, `room_admin` approve and quarantine release. `rfa human add|ls|rotate` manages them; the CLI acts as the first one. Verified live 2026-08-16: v0.5.0 migrated the standing room in place, pm-agent kept serving through the swap (11.6s round-trip), supervisor join + interrupt intervention delivered.

Room console: `rfa console --room <alias>` opens it. Observer = read-only live view; supervisor (human key) = intervention buttons, floor control, approve/reject, inject. Includes the live agent graph (canvas: members on a circle, kind-colored message pulses, decaying communication edges, task dots pool->owner->fade, ripples for presence/floor/interventions/gone_quiet; reduced-motion aware; toggle in the header). Verified live in a real browser 2026-08-16: history replay, live long-poll stream, inject with @mention (the pm-agent answered the injected chat and correctly refused it as out-of-scope), interrupt round-trip, and the graph animating a full scripted lifecycle.

- Claude Code: `rfa connect claude-code --room <alias> --skill` registers the hub with its own bearer and writes the consult-room skill into the project.
- Ask: `rfa ask "<question>"`; `--reply` continues that room's last conversation, so a challenge lands in the same brain session.
- Watch a room: `rfa room tail <alias> -f`, or the dashboard's Feed tab.
- Join info: `rfa room show <alias> --json`.
- Knowledge: `rfa knowledge status` says what each pack reads and flags a page that exists twice; a git remote is a tracked clone under the pack, never a copied export.

## Known limitations and parked edges

- **Moderation implemented (0.1.5) + the policy gate (0.1.7)**. Still unimplemented by design: `policies.join: "approve"`, `message_ttl_s`; floor state is restart-transient. Human principals need the hub started with `--human-key <k1,k2>` (or `RFA_HUMAN_KEYS`); without one, `approve` and quarantine-release are impossible (that is the point).
- **Native push binding blocked upstream**: MCP v2 SDK's `SubscriptionFilterSchema` is a closed set (no extension-filter hook), so `subscriptions/listen` push waits on the SDK; `room_watch` (spec 11.2b) is the binding. Watch `McpHttpHandler.notify` as the future attachment point.
- `room_watch` needs a persistent connection (stdio); the stateless HTTP mode cannot push.
- Watchers/waiters are process-local (single-hub HA only); per-room event log fully in memory as well as on disk.
- `ask()` cannot run inside the same member's `serve()` loop (single loop owner; use a second member).
- Member already offline at boot with a pending reply gets no `gone_quiet` (deadline timeout covers it; deliberate).

## Gotchas for future sessions

- **A non-interactive shell gets fnm's default Node v20, not the project's.** The repo needs >= 22 and the better-sqlite3 binding is built for v24.19.0 (ABI 137); under Node 20 the CLI preflight refuses init and every store test fails on the binding. Worse, on 2026-08-25 a missing dependency under Node 20 turned `npm test` into a silent multi-hour hang (askreply's hub outliving a crashed before hook; that hook is guarded now). Prefix gate runs with `PATH="$HOME/.local/share/fnm/node-versions/v24.19.0/installation/bin:$PATH"`.
- Bash `cd` persists between calls; a leaked cd into node_modules once caused scary false alarms. Prefer absolute paths.
- Scratchpad `.ts` scripts run as CJS; name them `.mts` for top-level await.
- Envelope `message_id` minimum 8 chars; the v2 SDK returns input-validation failures as PLAIN TEXT tool results (not JSON).
- Claim-less legacy HTTP posts get SSE-framed responses (`data:` lines); scripted probes should speak the modern era (`Mcp-Method`/`Mcp-Name` headers + `_meta` protocolVersion) for plain JSON.
- Testing agent flows non-interactively: `claude -p "<prompt or /command>" --model sonnet --allowedTools "Skill,Read,mcp__rfa-hub__room_join,mcp__rfa-hub__room_send,mcp__rfa-hub__room_listen,mcp__rfa-hub__room_roster"`.
- Never commit `reports/` or any hub directory's `.rfa/` (gitignored on purpose: reports, secrets, runtime state). `dogfood/` and `data/` no longer exist in the repository; an on-disk `dogfood/` is untracked residue, not source.
- Anti-accretion: when a rung flips to DONE its story moves to `docs/HISTORY.md` in the same commit, and findings are written to `docs/LEDGER.md` with at most a one-line pointer here; `test/statusclaims.test.ts` fails this file past 250 lines.
